# Copy to .env (or .env.local) — Expo inlines EXPO_PUBLIC_* vars at build time.

# Your Supabase project URL, e.g. https://abcdefgh.supabase.co
EXPO_PUBLIC_SUPABASE_URL=

# The project's anon (public) API key — safe to ship in the client; RLS enforces access.
EXPO_PUBLIC_SUPABASE_ANON_KEY=
